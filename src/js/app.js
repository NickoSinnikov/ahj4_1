// TODO: write code here
import cardValidateWidget from "./cardValidateWidget";

document.addEventListener("DOMContentLoaded", () => {
  const cardValid = new cardValidateWidget();
  cardValid.init();
});
